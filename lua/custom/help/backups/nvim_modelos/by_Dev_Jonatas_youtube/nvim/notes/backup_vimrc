

local function set_globals()
  vim.opt.mapleader = " "
end

local function set_mappings()

  local opts = { noremap = true } -- não remapear atalho que já existe

  local mappings = {

    

    -- Navegacao dos Buffers
    {"n", "<leader><up>", "<C-w><up><CR>", opts},
    {"n", "<leader><down>", "<C-w><down><CR>", opts}, -- todo restante
  }
    for _, val in pairs(mappings) do
      api.nvim_set_keymap(unpack(val))
    end

end

-- codes options by vimscript


  -- todo
  vim.cmd([[
  ]])

-- end